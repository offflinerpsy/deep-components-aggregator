---
applyTo: '**'
---
# Global Workflow: R→I→P + Context7 Integration

## 🎯 Применение

Это правило применяется **ко всем задачам во всех проектах**, где доступен MCP Context7.

---

## 🔄 Обязательный цикл работы: R→I→P

### R (Research — Разведка)

**Перед началом ЛЮБОЙ задачи**:

1. **Анализ требований**:
   - Что именно нужно сделать?
   - Какие технологии/библиотеки задействованы?
   - Есть ли примеры в текущем проекте?

2. **Context7 разведка** (ОБЯЗАТЕЛЬНО):
   ```
   mcp_context7_resolve-library-id → найти актуальную библиотеку
   mcp_context7_get-library-docs → получить свежую документацию
   ```
   
   **Что искать**:
   - Самые новые версии библиотек
   - Актуальные API и паттерны
   - Breaking changes в последних релизах
   - Официальные примеры кода
   - Best practices из документации

3. **Проверка проекта**:
   - `read_file` — изучить существующий код
   - `grep_search` — найти похожие решения
   - `semantic_search` — понять архитектуру
   - `file_search` — найти конфигурационные файлы

---

### I (Implementation Planning — План реализации)

1. **Анализ конфликтов**:
   - Проверить `package.json` / `requirements.txt`
   - Найти потенциальные конфликты версий
   - Убедиться в совместимости с существующим кодом
   - Проверить deprecated API

2. **Context7 верификация** (ПЕРЕД КАЖДЫМ ДЕЙСТВИЕМ):
   ```
   Перед созданием файла → Context7: проверить актуальный паттерн
   Перед использованием API → Context7: проверить синтаксис
   Перед установкой зависимости → Context7: проверить версию
   ```

3. **Создать TODO list**:
   ```
   manage_todo_list → write
   ```
   - Разбить задачу на атомарные шаги
   - Каждый шаг должен быть проверяемым
   - Отметить зависимости между шагами

---

### P (Proof — Доказательство)

1. **Реализация с проверками**:
   - Выполнить один TODO пункт
   - Проверить результат (тесты/артефакты)
   - Отметить как выполненный
   - Перейти к следующему

2. **Артефакты**:
   - Сохранять доказательства в `docs/_artifacts/<date>/`
   - Скриншоты, логи, JSON-результаты
   - Видео (если UI/UX изменения)

3. **Если что-то не работает** → ПОВТОР ЦИКЛА:
   ```
   Проблема обнаружена
      ↓
   R: Context7 + анализ ошибки
      ↓
   I: новый план решения
      ↓
   P: новая попытка реализации
      ↓
   Проверка → OK? → Продолжить | FAIL? → Повтор R→I→P
   ```

---

## 🔁 Диаграмма цикла

```
┌─────────────────────────────────────────────────────────┐
│  ЗАДАЧА ПОСТАВЛЕНА                                      │
└────────────────┬────────────────────────────────────────┘
                 ↓
        ┌────────────────┐
        │  R (Research)  │
        │  ─────────────  │
        │  • Анализ      │
        │  • Context7    │
        │  • Проверка    │
        └────────┬───────┘
                 ↓
     ┌──────────────────────┐
     │  I (Implementation)  │
     │  ───────────────────  │
     │  • Конфликты         │
     │  • Context7 verify   │
     │  • TODO list         │
     └──────────┬───────────┘
                ↓
        ┌──────────────┐
        │  P (Proof)   │
        │  ──────────   │
        │  • Реализация │
        │  • Тесты      │
        │  • Артефакты  │
        └──────┬───────┘
               ↓
          ┌─────────┐
          │ Работает?│
          └────┬─────┘
               ├─── ✅ ДА → ГОТОВО
               │
               └─── ❌ НЕТ → Анализ проблемы
                              ↓
                         Context7 проверка
                              ↓
                         Вернуться к R
                              ↓
                         Повторить I→P
```

---

## ❌ Запрещённые практики

**НИКОГДА не делать**:
- ❌ Сразу писать код без Context7 разведки
- ❌ Использовать паттерны из памяти без проверки
- ❌ Гадать какая версия библиотеки актуальна
- ❌ Копировать deprecated API
- ❌ Игнорировать breaking changes
- ❌ Пропускать TODO list для сложных задач
- ❌ Работать без артефактов

---

## ✅ Обязательные практики

**ВСЕГДА делать**:
- ✅ Context7 ПЕРЕД началом любой задачи
- ✅ Context7 ПЕРЕД каждым значимым действием
- ✅ Context7 при возникновении проблем
- ✅ Проверка конфликтов зависимостей
- ✅ TODO list для задач >2 шагов
- ✅ Артефакты для всех изменений
- ✅ Повторный цикл R→I→P при неудаче

---

## 📝 Примеры применения

### Пример 1: Добавить новую библиотеку

```
R (Research):
  1. Анализ: нужно добавить валидацию форм
  2. Context7: resolve-library-id "react-hook-form"
  3. Context7: get-library-docs для последней версии
  4. Проверка: grep_search "useForm" в проекте

I (Implementation):
  1. Проверка package.json: нет конфликтов
  2. Context7: актуальный синтаксис для v8.x
  3. TODO list:
     - Установить react-hook-form@latest
     - Создать useFormValidation hook
     - Интегрировать в форму поиска
     - Написать тест

P (Proof):
  1. npm install react-hook-form
  2. Создать hook по примеру из Context7
  3. Тест: форма валидирует корректно
  4. Артефакт: screenshot + test-results.json
```

### Пример 2: Исправить баг

```
R (Research):
  1. Анализ: loader не виден при навигации
  2. Проверка: read_file NavigationOverlay.tsx
  3. Context7: "react context state updates"
  4. Grep: найти все использования useNavigationOverlay

I (Implementation):
  1. Конфликты: проверить Next.js App Router docs
  2. Context7: актуальные паттерны для client components
  3. TODO list:
     - Добавить debug логирование
     - Проверить Context propagation
     - Тест через Playwright

P (Proof):
  1. Добавить console.log в begin()
  2. Playwright тест: проверить opacity
  3. Артефакт: screenshots + video
  4. Если не работает → R (Context7: альтернативные решения)
```

---

## 🔧 Интеграция с Tech Lead Mode

Цикл R→I→P **дополняет** существующий Tech Lead workflow:

```
Tech Lead Mode:          R→I→P Cycle:
─────────────────        ─────────────
PLAN                  ←  R (Research)
CHANGES               ←  I (Implementation Planning)
RUN                   ←  P (Proof) - выполнение
VERIFY                ←  P (Proof) - проверка
ARTIFACTS             ←  P (Proof) - доказательства
GIT                   ←  После успешного P
```

**Когда что использовать**:
- **R→I→P**: разведка, планирование, итеративное решение
- **Tech Lead Mode**: финальная структура ответа, документирование

---

## 🎓 Обучение через примеры

При каждом использовании Context7 создавать заметки:

```markdown
# Context7 Learning Log

## 2025-11-06: React Context + State Updates

**Query**: "react context state propagation in client components"
**Library**: react@18.3.0

**Findings**:
- Context updates trigger re-renders only in consuming components
- useState in provider doesn't guarantee immediate visual updates
- Need useEffect to log state changes for debugging

**Applied to**: NavigationOverlay.tsx debugging
**Result**: Identified opacity issue through console logs
```

---

## 📚 Ссылки

- **Context7 Documentation**: https://context7.dev/docs
- **MCP Protocol**: https://modelcontextprotocol.io
- **Best Practices**: `.github/copilot-instructions.md`

---

**Версия**: 1.0.0  
**Дата**: 6 ноября 2025  
**Автор**: offflinerpsy  
**Статус**: ACTIVE — применяется ко всем задачам
