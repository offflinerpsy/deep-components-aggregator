# R→I→P + Context7: Краткая справка

## 🎯 Суть

**Перед ЛЮБОЙ задачей**: Research → Implementation → Proof + Context7 на каждом шаге

---

## 📍 Где находятся правила

### 1. Локальное (этот проект)
```
.github/copilot-instructions.md
```
Секция: `## 🔄 ОБЯЗАТЕЛЬНЫЙ ЦИКЛ: R→I→P + Context7 (ПОСТОЯННО)`

### 2. Глобальное (все проекты)
```
.github/instructions/context7-workflow.instructions.md
```
`applyTo: '**'` — применяется везде

---

## ⚡ Быстрая шпаргалка

### R (Research)
```bash
1. Что нужно сделать?
2. Context7: resolve-library-id → get-library-docs
3. read_file / grep_search / semantic_search
```

### I (Implementation)
```bash
1. Проверка конфликтов (package.json)
2. Context7: актуальный синтаксис/паттерн
3. manage_todo_list → создать план
```

### P (Proof)
```bash
1. Выполнить TODO пункт
2. Проверить через тесты
3. Сохранить артефакт
4. НЕ РАБОТАЕТ? → R (Context7) → I (новый план) → P (повтор)
```

---

## 🔁 Цикл при неудаче

```
Проблема → R (Context7 + анализ) → I (план) → P (попытка)
   ↑                                              ↓
   ← ← ← ← ← ← ← Не работает? ← ← ← ← ← ← ← ← ← ←
```

---

## ❌ Запрещено

- Сразу писать код без Context7
- Использовать код из памяти
- Гадать версию библиотеки
- Пропускать TODO list

---

## ✅ Обязательно

- Context7 ПЕРЕД началом
- Context7 ПЕРЕД каждым действием
- Context7 при проблемах
- TODO list для задач
- Артефакты всегда

---

## 🔄 Как перезагрузить правила

```bash
# 1. Закрыть VS Code
# 2. Открыть VS Code
# 3. Правила подхватятся автоматически
```

**Проверка**:
- `.github/copilot-instructions.md` → локальные правила
- `.github/instructions/*.instructions.md` → глобальные

---

## 📝 Commit

```
7fc526e - docs(workflow): add mandatory R→I→P + Context7 cycle for all tasks
```

**Версия**: 1.2.0  
**Дата**: 6 ноября 2025  
**Статус**: ✅ АКТИВНО

---

## 🎓 Примеры

### Задача: Добавить библиотеку

```
R: Context7 → "react-hook-form" latest docs
I: Проверка package.json → TODO list (4 шага)
P: Установка → Реализация → Тест → Артефакт
```

### Задача: Исправить баг

```
R: read_file + Context7 "react context state"
I: Проверка конфликтов → TODO (3 шага)
P: Debug logs → Playwright тест → Скриншоты
   ↓ Не работает?
R: Context7 "alternative patterns"
I: Новый план
P: Повторная попытка
```

---

**Готово!** Перезагрузи VS Code для активации правил.
