# ✅ Отчёт: Внедрение R→I→P + Context7 Workflow

**Дата**: 6 ноября 2025  
**Коммиты**: `7fc526e`, `a914caf`  
**Статус**: ✅ ГОТОВО — правила активны

---

## 🎯 Что сделано

### 1. Локальное правило проекта
**Файл**: `.github/copilot-instructions.md`

**Добавлена секция** (строки 13-90):
```markdown
## 🔄 ОБЯЗАТЕЛЬНЫЙ ЦИКЛ: R→I→P + Context7 (ПОСТОЯННО)

### R (Research — Разведка)
- Первичный анализ задачи
- Context7 разведка (ОБЯЗАТЕЛЬНО)
- Проверка репозитория

### I (Implementation Planning — План реализации)
- Анализ конфликтов
- Context7 верификация (ПЕРЕД КАЖДЫМ ДЕЙСТВИЕМ)
- Составить финальный план + TODO list

### P (Proof — Доказательство)
- Реализация с проверками
- Если не получается → ПОВТОР ЦИКЛА
```

**Версия**: 1.1.0 → 1.2.0

---

### 2. Глобальное правило для всех проектов
**Файл**: `.github/instructions/context7-workflow.instructions.md`

**Структура** (400+ строк):
```
applyTo: '**'

- Обязательный цикл работы: R→I→P
- Диаграмма цикла с визуализацией
- Запрещённые/обязательные практики
- Примеры применения (2 use-case)
- Интеграция с Tech Lead Mode
- Learning log для Context7
```

**Применение**: ВСЕ задачи во ВСЕХ проектах

---

### 3. Шпаргалка для быстрого доступа
**Файл**: `docs/CONTEXT7-WORKFLOW-CHEATSHEET.md`

**Содержание**:
- Где находятся правила
- Быстрая шпаргалка R→I→P
- Цикл при неудаче
- Запреты/обязательства
- Примеры задач

---

## 📝 Commits

### Commit 1: `7fc526e`
```
docs(workflow): add mandatory R→I→P + Context7 cycle for all tasks

- Added R→I→P workflow to copilot-instructions.md
- Created global context7-workflow.instructions.md
- R (Research): Context7 recon + repo analysis
- I (Implementation): conflict check + verification + TODO list
- P (Proof): implementation + artifacts + retry loop
- Applies to ALL tasks in ALL projects
- Version: 1.2.0

Files:
  modified: .github/copilot-instructions.md (+86 lines)
  created:  .github/instructions/context7-workflow.instructions.md (358 lines)
```

### Commit 2: `a914caf`
```
docs(workflow): add quick reference cheatsheet for R→I→P cycle

Files:
  created: docs/CONTEXT7-WORKFLOW-CHEATSHEET.md (130 lines)
```

---

## 🗂️ Структура файлов

```
/opt/deep-agg/
├─ .github/
│  ├─ copilot-instructions.md           ← ЛОКАЛЬНОЕ правило
│  └─ instructions/
│     ├─ context7-workflow.instructions.md  ← ГЛОБАЛЬНОЕ правило
│     └─ russia.instructions.md
└─ docs/
   └─ CONTEXT7-WORKFLOW-CHEATSHEET.md   ← Шпаргалка
```

---

## ✅ Как работают правила

### Автоматическая загрузка

**VS Code Copilot** при старте читает:

1. **Глобальные правила**:
   ```
   .github/instructions/*.instructions.md
   ```
   Файлы с `applyTo: '**'` применяются ко всем файлам проекта

2. **Локальные правила**:
   ```
   .github/copilot-instructions.md
   ```
   Применяются ко всему проекту по умолчанию

3. **Приоритет**:
   - Локальные дополняют глобальные
   - Конфликтов нет (правила независимые)

### Активация

```bash
# 1. Файлы уже закоммичены → Git видит изменения
# 2. Закрыть VS Code
# 3. Открыть VS Code
# 4. Copilot автоматически прочитает новые правила
```

**Проверка активации**:
- В Copilot Chat: "какие правила ты используешь?"
- Ответ должен содержать: "R→I→P цикл", "Context7 разведка"

---

## 🔄 Workflow в действии

### Пример задачи: Исправить loader visibility

**БЕЗ R→I→P** (старый подход):
```
1. Пользователь: "не работает"
2. Агент: добавляет setTimeout
3. Не работает → добавляет console.log
4. Не работает → меняет CSS
5. Не работает → ... (бесконечный цикл)
```

**С R→I→P** (новый подход):
```
R (Research):
  - Анализ: loader существует но невидим
  - Context7: "react context state propagation"
  - read_file: NavigationOverlay.tsx
  - grep_search: "useNavigationOverlay"
  - ВЫВОД: opacity:0 вместо opacity:1

I (Implementation):
  - Конфликты: Next.js 14 + React 18 совместимы
  - Context7: "react useState debugging patterns"
  - TODO list:
    1. Добавить debug logging
    2. Проверить Context propagation
    3. Playwright тест
    4. Проверить Tailwind classes

P (Proof):
  - Добавить console.log в begin()
  - Playwright тест → opacity: '0' (баг подтверждён)
  - Артефакты: 7 screenshots + video
  
  НЕ РАБОТАЕТ?
  ↓
R (повтор):
  - Context7: "react context not updating component"
  - Нашли: useEffect dependency missing
  
I (новый план):
  - TODO: добавить useEffect([active])
  
P (новая попытка):
  - Реализация → Тест → ✅ РАБОТАЕТ
```

---

## 📊 Преимущества

### До внедрения

- ❌ Код из памяти (устаревший)
- ❌ Догадки вместо фактов
- ❌ Нет проверки конфликтов
- ❌ Хаотичное решение проблем

### После внедрения

- ✅ Context7: актуальная документация
- ✅ Проверка репозитория перед действием
- ✅ Анализ конфликтов заранее
- ✅ Структурированный retry при неудаче
- ✅ TODO list для отслеживания прогресса
- ✅ Артефакты на каждом шаге

---

## 🎓 Обучение агента

Правила содержат механизм **самообучения**:

```markdown
# Context7 Learning Log

## 2025-11-06: React Context + State Updates

**Query**: "react context state propagation"
**Library**: react@18.3.0

**Findings**:
- Context updates trigger re-renders only in consuming components
- useState doesn't guarantee immediate visual updates
- Need useEffect for debugging state changes

**Applied to**: NavigationOverlay.tsx
**Result**: Found opacity:0 root cause
```

Каждый цикл R→I→P накапливает знания.

---

## 🔍 Проверочный чек-лист

Перед началом работы агент должен:

- [ ] Прочитать Context7 docs для библиотеки
- [ ] Проверить актуальность версии
- [ ] Найти примеры в текущем проекте
- [ ] Проверить package.json на конфликты
- [ ] Создать TODO list (если задача >2 шагов)
- [ ] Сохранить артефакты после каждого шага
- [ ] При неудаче → повторить R→I→P

---

## 🚀 Следующие шаги

1. **Перезагрузить VS Code** для активации правил

2. **Проверить работу** на текущей задаче (loader fix):
   ```
   R: Context7 "react opacity transition debugging"
   I: План исправления opacity:0
   P: Реализация + Playwright тест
   ```

3. **Использовать шпаргалку**:
   ```
   docs/CONTEXT7-WORKFLOW-CHEATSHEET.md
   ```

4. **Обновлять Learning Log** при каждом использовании Context7

---

## 📌 Ссылки

- **Локальное правило**: `.github/copilot-instructions.md` (строки 13-90)
- **Глобальное правило**: `.github/instructions/context7-workflow.instructions.md`
- **Шпаргалка**: `docs/CONTEXT7-WORKFLOW-CHEATSHEET.md`
- **Context7 MCP**: доступен через `mcp_context7_*` tools

---

## ✅ Результат

**Что изменилось**:

| Аспект | Было | Стало |
|--------|------|-------|
| Подход | Хаотичный | Структурированный R→I→P |
| Источник | Память агента | Context7 + официальные docs |
| Проверка | После реализации | ПЕРЕД каждым действием |
| Конфликты | Находятся постфактум | Проверяются заранее |
| При ошибке | Случайные попытки | Повтор R→I→P цикла |
| TODO | Опционально | Обязательно для задач |
| Артефакты | Иногда | Всегда |

**Статус**: ✅ ГОТОВО — правила закоммичены и готовы к использованию

---

**Автор**: offflinerpsy + GitHub Copilot  
**Дата**: 6 ноября 2025, 11:30 UTC  
**Версия правил**: 1.2.0
