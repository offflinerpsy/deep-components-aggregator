# ИСТОЧНИК ПРАВДЫ: Откуда берутся цены и склады

**Дата**: 21 декабря 2025  
**Триггер**: Обнаружена ошибка в документации (упоминание OEMstrade API для цен)  
**Статус**: ✅ ПРАВДА НАЙДЕНА

---

## ❌ ЧТО БЫЛО НЕПРАВИЛЬНО

### В старой документации (README.md, ARCHITECTURE.md):
```
Источники данных:
- Контент (описания/спеки/изображения/доки): DigiKey, Mouser, Farnell, TME
- Цены/склады: OEMstrade API (региональные данные)  ← ЭТО НЕПРАВДА!
```

### В `.github/instructions/russia.instructions.md`:
```
Цены/склады агрегируются через OEMstrade.  ← ЭТО НЕПРАВДА!
```

---

## ✅ РЕАЛЬНОСТЬ: Откуда берутся цены

### 1. Основные источники (API с ценами):

#### DigiKey API (Products V4)
- **Эндпоинт**: `https://api.digikey.com/products/v4/search/keyword`
- **Цены**: `StandardPricing[]` с `UnitPrice` и `BreakQuantity`
- **Валюта**: USD (по умолчанию)
- **Склады**: `QuantityAvailable`
- **Файл**: `src/integrations/digikey/client.mjs` + `normalize.mjs`

**Пример структуры**:
```javascript
pricing: [{
  qty: 1,
  price: 0.47,
  currency: 'USD',
  price_rub: 45.23  // конвертировано через ЦБ РФ
}]
```

#### Mouser API
- **Эндпоинт**: `https://api.mouser.com/api/v1/search/keyword`
- **Цены**: `PriceBreaks[]` с `Price` и `Quantity`
- **Валюта**: USD, EUR, GBP (детектится по символу `€`, `£`)
- **Склады**: `Availability` (парсится число)
- **Файл**: `src/integrations/mouser/client.mjs` + `normalize.mjs`

**Пример структуры**:
```javascript
price_breaks: [{
  qty: 1,
  price: 0.52,
  currency: 'USD',
  price_rub: 50.04  // конвертировано через ЦБ РФ
}]
```

#### TME API
- **Эндпоинт**: TME OAuth API
- **Цены**: `PriceList[]` с ценами и количествами
- **Валюта**: EUR, PLN, USD
- **Файл**: `src/integrations/tme/client.mjs` + `normalize.mjs`

#### Farnell API
- **Эндпоинт**: Farnell Partner API
- **Цены**: `prices[]` с `cost`, `breakPrice`, `unitPrice`
- **Валюта**: GBP, EUR
- **Файл**: `src/integrations/farnell/client.mjs` + `normalize.mjs`

---

### 2. Конвертация валют

**Источник курсов**: Центральный Банк России (ЦБ РФ) API
- **URL**: `https://www.cbr-xml-daily.ru/daily_json.js`
- **Обновление**: Каждые 24 часа
- **Валюты**: USD, EUR, GBP, CNY, JPY и др.
- **Файл**: `src/currency/cbr.mjs`

**Функция конвертации**:
```javascript
import { toRub } from '../../currency/cbr.mjs';

const priceRub = toRub(unitPrice, 'USD');  // USD → RUB
```

---

### 3. Ручные товары (Manual Products)

**Источник**: SQLite база `var/db/deepagg.sqlite`  
**Таблица**: `manual_products`  
**Файл**: `src/search/manualProducts.mjs`

**Структура**:
```sql
CREATE TABLE manual_products (
  mpn TEXT PRIMARY KEY,
  manufacturer TEXT,
  description TEXT,
  price REAL,           -- цена вводится вручную
  currency TEXT,        -- RUB, USD, EUR, GBP
  stock INTEGER,        -- склад вводится вручную
  region TEXT,
  pricing TEXT,         -- JSON массив ценовых уровней
  availability TEXT     -- JSON данные о наличии
);
```

**Применение наценки**:
```javascript
import { applyMarkup } from '../utils/markup.mjs';

const finalPrice = applyMarkup(row.price, row.currency);
```

---

### 4. Наценка (Markup)

**Источник**: SQLite `settings` таблица  
**Ключ**: `pricing_policy`  
**Файл**: `src/utils/markup.mjs`

**Дефолтное значение**:
```json
{
  "markup_percent": 0.30,  // 30%
  "markup_fixed_rub": 500  // +500 руб
}
```

**Формула**:
```javascript
finalPrice = Math.ceil(basePrice * (1 + markup_percent) + markup_fixed_rub)
```

**Пример**:
- Цена от DigiKey: $1.00 (USD)
- Курс ЦБ РФ: 96.50 руб
- Базовая цена: 96.50 руб
- Наценка 30%: 96.50 × 1.3 = 125.45 руб
- Фикс. наценка: 125.45 + 500 = 625.45 руб
- **Финальная цена**: 626 руб (округлено вверх)

---

## 🌐 ЧТО ТАКОЕ OEMstrade В РЕАЛЬНОСТИ

OEMstrade — это **НЕ API источник**, а:

### 1. Источник товаров для витрины
**URL**: `https://www.oemstrade.com/most_searched`  
**Использование**: Скрейпинг топовых товаров для главной страницы  
**Файл**: `docs/_artifacts/2025-11-04-vitrine-update/VITRINE-UPDATE-REPORT.md`

**Что берётся**:
- MPN топовых компонентов
- Список популярных товаров
- **НЕ берутся цены/склады!**

### 2. Генерация ссылок для админки
**Формат**: `https://www.oemstrade.com/search/${mpn}`  
**Использование**: Ссылки для администраторов (не для пользователей)  
**Место**: `dealer_links` JSON в таблице `orders`

**Пример**:
```json
{
  "suppliersLinks": [
    "https://oemstrade.com/search?q=CRCW040210K0FKED"
  ]
}
```

### 3. UI-референс для дизайна
**Использование**: Карточка товара вдохновлена дизайном OEMstrade  
**Источник**: `docs/VIDEO-REQUIREMENTS.md` (BLOCK 2)

**Что скопировано**:
- Структура карточки товара
- Таблица цен с quantity breaks
- Sticky sidebar layout
- **НЕ API интеграция!**

---

## 📊 РЕАЛЬНАЯ СХЕМА DATA FLOW

```
Пользователь вводит: "LM317T"
    ↓
Backend /api/search?q=LM317T
    ↓
Параллельные запросы:
    ├── DigiKey API → цены USD → конвертация ЦБ РФ → price_rub
    ├── Mouser API → цены USD/EUR → конвертация ЦБ РФ → price_rub
    ├── TME API → цены EUR/PLN → конвертация ЦБ РФ → price_rub
    ├── Farnell API → цены GBP/EUR → конвертация ЦБ РФ → price_rub
    └── Manual Products (SQLite) → price + currency → наценка → price_rub
    ↓
Агрегация + нормализация (выбор min_price_rub)
    ↓
Применение наценки markup_percent + markup_fixed_rub
    ↓
Кэш в SQLite (7 дней)
    ↓
Response: { rows: [...], min_price_rub: 626 }
```

---

## 🔧 ФАЙЛЫ С ЦЕНОВОЙ ЛОГИКОЙ

### Интеграции (цены из API):
- `src/integrations/digikey/client.mjs` + `normalize.mjs`
- `src/integrations/mouser/client.mjs` + `normalize.mjs`
- `src/integrations/tme/client.mjs` + `normalize.mjs`
- `src/integrations/farnell/client.mjs` + `normalize.mjs`

### Конвертация валют:
- `src/currency/cbr.mjs` — Центробанк РФ API
- `src/currency/toRUB.mjs` — Универсальный конвертер

### Наценка:
- `src/utils/markup.mjs` — Применение markup_percent + markup_fixed_rub

### Ручные товары:
- `src/search/manualProducts.mjs` — Поиск в SQLite + применение наценки

### API роуты:
- `src/api/product.mjs` — Извлечение и нормализация price_breaks
- `api/search-reasons.mjs` — Оркестратор поиска с ценами

---

## 📝 ЧТО НУЖНО ИСПРАВИТЬ

### 1. Документация
- [ ] README.md — убрать "Цены/склады: OEMstrade API"
- [ ] ARCHITECTURE.md — убрать "OEMstrade API (цены/остатки)"
- [ ] ПОЛНЫЙ-ГАЙД-ПО-ПРОЕКТУ.md — исправить раздел источников
- [ ] .github/instructions/russia.instructions.md — исправить "Mission & Truth"

### 2. Правильная формулировка:
```markdown
## Источники данных

**Контент + Цены + Склады**:
- DigiKey API (Products V4) — спеки, цены USD, изображения, даташиты
- Mouser API — спеки, цены USD/EUR, изображения, даташиты
- TME API — спеки, цены EUR/PLN
- Farnell API — спеки, цены GBP/EUR

**Конвертация валют**:
- Центральный Банк России API (курсы валют, обновление каждые 24ч)

**Дополнительные источники**:
- Manual Products (SQLite) — ручные товары с произвольными ценами
- OEMstrade most_searched — топовые товары для витрины (скрейпинг, не API)

**Что показывается пользователю**:
- Регион поставки (US/EU/GLOBAL)
- Цена в рублях (с наценкой)
- Наличие на складе
- MPN, производитель, описание, даташиты

**Что только в админке**:
- Ссылки на дилеров (DigiKey/Mouser/Farnell/OEMstrade)
- Наценка (markup_percent, markup_fixed_rub)
- Кэш-статистика
```

---

## ✅ ИТОГОВАЯ ПРАВДА

| Вопрос | Ответ |
|--------|-------|
| Откуда цены? | DigiKey API, Mouser API, TME API, Farnell API + Manual Products (SQLite) |
| Откуда склады? | Те же API (QuantityAvailable/Availability) + Manual Products |
| Есть ли OEMstrade API? | **НЕТ!** Это не API-источник |
| Что такое OEMstrade в проекте? | 1) Скрейпинг топов для витрины 2) Генератор ссылок для админки 3) UI-референс |
| Откуда курсы валют? | Центральный Банк России (ЦБ РФ) API |
| Как применяется наценка? | `finalPrice = ceil(basePrice * (1 + 0.30) + 500)` |

---

**Вывод**: Вся старая документация с упоминанием "OEMstrade API для цен" — **НЕПРАВДА**.  
Реальные источники: **DigiKey/Mouser/TME/Farnell API** + конвертация через **ЦБ РФ** + **наценка из настроек**.

---

**Автор**: GitHub Copilot (Claude Sonnet 4.5)  
**Источник правды**: Анализ кода + git history + docs/_artifacts/
