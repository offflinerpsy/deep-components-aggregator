# ТЗ: Редизайн карточки товара — Скрытие поставщиков

**Дата**: 21 декабря 2025  
**Статус**: В работе  
**Приоритет**: ВЫСОКИЙ

---

## 🎯 Цель

Убрать ВСЕ упоминания реальных поставщиков (Mouser, DigiKey, Farnell, TME) из UI. Пользователь должен видеть **агрегированные предложения** без привязки к источнику данных.

---

## 📋 Требования

### 1. **Убрать упоминания поставщиков**

**КРИТИЧНО**: Нигде не должно быть видно:
- ❌ "US", "EU", "GLOBAL" (регионы = leak поставщика)
- ❌ "Mouser", "DigiKey", "Farnell", "TME" (названия)
- ❌ Прямые ссылки на сайты поставщиков
- ❌ Любые идентификаторы поставщиков в URL/UI

**Заменить на:**
- ✅ "Склад 1", "Склад 2", "Склад 3"
- ✅ Агрегированные данные без привязки к источнику

---

### 2. **Верхняя часть карточки (Hero Section)**

**Было:**
```
В наличии: 710 шт
ДОСТУПНО В РЕГИОНАХ
US
```

**Стало:**
```
✅ Доступно: 710+ шт (на 3 складах)
Срок: от 3 дней
```

**Изменения:**
- Убрать секцию "ДОСТУПНО В РЕГИОНАХ" полностью
- "В наличии" → "Доступно"
- Добавить "(на N складах)" если несколько источников
- Добавить "Срок: от X дней" (минимальный ETA)

---

### 3. **Градация цен (Price Breaks) — Вариант 3**

**Компактный формат с badges:**

```
┌─────────────────────────────────────────────────┐
│ Цена в зависимости от количества:              │
│                                                 │
│ [1 шт]      ₽545.98                            │
│ [10 шт]     ₽362.37  ⬇ -34%                    │
│ [25 шт]     ₽313.84  ⬇ -42%                    │
│ [100 шт]    ₽259.64  ⬇ -52%                    │
│ [250 шт]    ₽232.95  ⬇ -57%                    │
│ [500 шт]    ₽216.77  ⬇ -60% ⭐ Лучшая цена     │
└─────────────────────────────────────────────────┘
```

**Логика:**
- Показываем все price breaks от ВСЕХ поставщиков
- Сортируем по количеству (от меньшего к большему)
- Дедупликация по количеству (если несколько складов дают одинаковый тир)
- Показываем процент скидки от первого тира
- Выделяем лучшую цену (минимальная цена за единицу)

---

### 4. **Таблица складов (Warehouses Table)**

**Было:**
```
Регион | Цена (₽) | MOQ | ETA
US     | ₽545.98  | 1   | —
US     | ₽362.37  | 10  | —
```

**Стало:**
```
Склад       | Доступно | Цена (от)    | Срок
Склад 1     | 300 шт   | ₽216.77/шт   | В наличии
Склад 2     | 250 шт   | ₽232.95/шт   | 3-5 дней
Склад 3     | 160 шт   | ₽259.64/шт   | 7-10 дней
```

**При клике на склад** → раскрывается градация цен для этого склада:
```
▼ Склад 1 (300 шт, ₽216.77/шт, в наличии)
  1-9 шт     → ₽545.98/шт
  10-24 шт   → ₽362.37/шт
  25-99 шт   → ₽313.84/шт
  100-249 шт → ₽259.64/шт
  250-499 шт → ₽232.95/шт
  500+ шт    → ₽216.77/шт  ⭐ Лучшая цена
```

**Изменения:**
- ❌ Убрать столбец "Регион"
- ✅ Добавить столбец "Склад" (Склад 1, Склад 2, ...)
- ✅ "MOQ" → "Доступно" (показываем stock, а не MOQ)
- ✅ "ETA" → "Срок"
- ✅ "Цена (₽)" → "Цена (от)" + "/шт"

---

### 5. **Логика агрегации данных**

**Backend изменения (`src/api/product.mjs`):**

1. **Получаем данные от всех провайдеров:**
   ```javascript
   const offers = [
     { source: 'mouser', stock: 300, priceBreaks: [...], region: 'US' },
     { source: 'digikey', stock: 250, priceBreaks: [...], region: 'US' },
     { source: 'farnell', stock: 160, priceBreaks: [...], region: 'EU' }
   ]
   ```

2. **Скрываем source, присваиваем номер склада:**
   ```javascript
   const warehouses = offers.map((offer, idx) => ({
     id: `wh${idx + 1}`,           // Склад 1, 2, 3...
     name: `Склад ${idx + 1}`,
     stock: offer.stock,
     priceBreaks: offer.priceBreaks,
     minPrice: Math.min(...offer.priceBreaks.map(pb => pb.price_rub)),
     eta: calculateETA(offer.source, offer.region), // Вычисляем ETA без упоминания источника
     // НЕ возвращаем: source, region, product_url
   }))
   ```

3. **Агрегируем price breaks для градации:**
   ```javascript
   const allPriceBreaks = warehouses
     .flatMap(wh => wh.priceBreaks)
     .sort((a, b) => a.qty - b.qty)
     .reduce((acc, pb) => {
       // Дедупликация по qty, берём лучшую цену
       const existing = acc.find(x => x.qty === pb.qty);
       if (!existing || pb.price_rub < existing.price_rub) {
         return [...acc.filter(x => x.qty !== pb.qty), pb];
       }
       return acc;
     }, []);
   ```

4. **Вычисляем общее количество:**
   ```javascript
   const totalStock = warehouses.reduce((sum, wh) => sum + wh.stock, 0);
   const warehouseCount = warehouses.length;
   ```

5. **Возвращаем клиенту:**
   ```json
   {
     "mpn": "LTC3221EDC-3.3#TRMPBF",
     "manufacturer": "Analog Devices",
     "title": "...",
     "totalStock": 710,
     "warehouseCount": 3,
     "minETA": "3 дня",
     "bestPrice": 216.77,
     "priceBreaks": [
       { "qty": 1, "price_rub": 545.98, "discount": 0 },
       { "qty": 10, "price_rub": 362.37, "discount": 34 },
       ...
     ],
     "warehouses": [
       {
         "id": "wh1",
         "name": "Склад 1",
         "stock": 300,
         "minPrice": 216.77,
         "eta": "В наличии",
         "priceBreaks": [...]
       },
       ...
     ],
     // НЕ возвращаем: regions, sources, product_urls
   }
   ```

---

### 6. **Frontend изменения**

**Файл:** `frontend/app/product/[mpn]/page.tsx`

**Изменения:**

1. **Hero Section:**
   ```tsx
   <div className="availability">
     <CheckIcon /> Доступно: {data.totalStock}+ шт 
     {data.warehouseCount > 1 && ` (на ${data.warehouseCount} складах)`}
   </div>
   <div className="eta">
     Срок: от {data.minETA}
   </div>
   ```

2. **Price Breaks (Inline Badges):**
   ```tsx
   <div className="price-breaks">
     <h3>Цена в зависимости от количества:</h3>
     {data.priceBreaks.map(pb => (
       <div key={pb.qty} className="price-badge">
         <span className="qty">{pb.qty}+ шт</span>
         <span className="price">₽{pb.price_rub.toFixed(2)}</span>
         {pb.discount > 0 && (
           <span className="discount">⬇ -{pb.discount}%</span>
         )}
         {pb.price_rub === data.bestPrice && (
           <span className="best-badge">⭐ Лучшая цена</span>
         )}
       </div>
     ))}
   </div>
   ```

3. **Warehouses Table:**
   ```tsx
   <table className="warehouses-table">
     <thead>
       <tr>
         <th>Склад</th>
         <th>Доступно</th>
         <th>Цена (от)</th>
         <th>Срок</th>
       </tr>
     </thead>
     <tbody>
       {data.warehouses.map(wh => (
         <tr key={wh.id} onClick={() => toggleWarehouse(wh.id)}>
           <td>{wh.name}</td>
           <td>{wh.stock} шт</td>
           <td>₽{wh.minPrice.toFixed(2)}/шт</td>
           <td>{wh.eta}</td>
         </tr>
       ))}
     </tbody>
   </table>
   ```

4. **Убрать:**
   - ❌ Секцию "ДОСТУПНО В РЕГИОНАХ"
   - ❌ Любые упоминания "US", "EU", "GLOBAL"
   - ❌ Ссылки на product_url поставщиков
   - ❌ Логотипы/иконки поставщиков

---

### 7. **ETA Mapping (Сроки доставки)**

**Вычисление ETA без упоминания поставщика:**

```javascript
function calculateETA(source, region, stock) {
  // Логика вычисления срока БЕЗ упоминания источника
  
  if (stock > 0) {
    // В наличии
    if (region === 'RU') return 'В наличии';
    if (region === 'EU') return '3-5 дней';
    if (region === 'US') return '7-10 дней';
  }
  
  // Под заказ
  if (region === 'EU') return '10-14 дней';
  if (region === 'US') return '14-21 день';
  
  return 'Уточняйте';
}
```

**Возвращаем клиенту ТОЛЬКО срок**, без упоминания откуда данные.

---

### 8. **URL и ссылки**

**Убрать из API ответа:**
- ❌ `product_url` (прямая ссылка на Mouser/DigiKey)
- ❌ `source` (mouser/digikey/farnell/tme)
- ❌ `regions` (US/EU/GLOBAL)

**Оставить:**
- ✅ `datasheet_url` (PDF спецификации)
- ✅ `image_url` (изображение компонента)
- ✅ Внутренние ссылки проекта

---

## 🔒 Безопасность данных

**КРИТИЧНО**: Пользователь **НЕ ДОЛЖЕН** видеть:
- Откуда мы берём данные (Mouser, DigiKey, etc.)
- Прямые ссылки на сайты поставщиков
- Region codes (US/EU) как идентификатор поставщика

**Разрешено** показывать:
- Агрегированные цены
- Сроки доставки (без привязки к поставщику)
- Общее количество на складах
- Градацию цен (price breaks)

---

## ✅ Acceptance Criteria

- [ ] Убрана секция "ДОСТУПНО В РЕГИОНАХ"
- [ ] "В наличии" заменено на "Доступно (на N складах)"
- [ ] Таблица показывает "Склад 1", "Склад 2" вместо "US"
- [ ] Столбец "MOQ" заменён на "Доступно"
- [ ] Добавлена градация цен (inline badges) с процентами скидки
- [ ] Сроки доставки показываются без упоминания поставщика
- [ ] API не возвращает `source`, `regions`, `product_url`
- [ ] При клике на склад раскрывается его градация цен
- [ ] Нигде в UI не видно Mouser/DigiKey/Farnell/TME

---

## 📂 Файлы для изменения

**Backend:**
- [ ] `src/api/product.mjs` — агрегация данных, скрытие источников
- [ ] `src/integrations/*/normalize.mjs` — не возвращать лишнее

**Frontend:**
- [ ] `frontend/app/product/[mpn]/page.tsx` — редизайн UI
- [ ] `frontend/components/PriceBreaks.tsx` — компонент градации цен
- [ ] `frontend/components/WarehousesTable.tsx` — таблица складов
- [ ] `frontend/styles/product.css` — стили для badges

---

## 🚀 План реализации

1. **Backend** (1-2 часа):
   - Изменить `src/api/product.mjs`: агрегация, скрытие источников
   - Добавить `warehouses` вместо `regions`
   - Убрать `source`, `product_url` из ответа

2. **Frontend** (2-3 часа):
   - Редизайн Hero Section
   - Добавить inline badges для price breaks
   - Изменить таблицу: "Склад 1/2/3" вместо "US"
   - Убрать секцию "ДОСТУПНО В РЕГИОНАХ"

3. **Тестирование** (1 час):
   - Проверить на разных MPN
   - Убедиться что нет leak'ов поставщиков
   - Проверить мобильную версию

---

**Начинаем реализацию** → Backend → Frontend → Тестирование
