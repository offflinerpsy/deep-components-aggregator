# Отчёт: Проверка Page Loader

**Дата**: 5 ноября 2025, 16:06 UTC  
**Тестировщик**: Playwright Automated Tests  
**URL**: https://prosnab.tech/

---

## ✅ Результаты автоматизированного тестирования

### 📊 Playwright Test Results

```bash
✅ Лоадер появился
✅ 6 квадратиков видны
⏱️  Лоадер показывался 3077ms (~3.1 секунды)
📦 Квадратиков: 6/6
🎨 Цвет фона: rgba(255, 255, 255, 0.85)
```

### 📸 Визуальные доказательства

1. **01-before-submit.png** (78KB) — Главная страница перед отправкой формы
2. **02-loader-appears.png** (38KB) — ✅ **ЛОАДЕР ВИДЕН** через 100ms после submit
3. **03-loader-middle.png** (44KB) — ✅ **Градиентная анимация** в процессе (1.5s)
4. **04-results-shown.png** (105KB) — Результаты поиска после завершения

### 🎬 Видео запись

- **loader-test-recording.webm** (231KB) — Полная запись теста от начала до конца

---

## 🎨 Визуальные характеристики

### Лоадер (`.page-loader-overlay`):
- **Фон**: `rgba(255, 255, 255, 0.85)` — белый полупрозрачный
- **Размытие**: `backdrop-filter: blur(8px)` — видно контент сзади
- **Z-index**: `9999` — поверх всего контента
- **Анимация**: `fadeIn 0.3s ease-in` — плавное появление

### Градиентные квадратики (`.page-loader-box`):
- **Количество**: 6 штук (`.one` - `.six`)
- **Градиент**: `linear-gradient(to right, #141562, #486FBC, #EAB5A1, #8DD6FF, #4973C9, #D07CA7, #F4915E, #F5919E, #B46F89, #141562, #486FBC)`
- **Анимация**: 
  - `moveGradient 15s infinite` — движение градиента
  - `oneMove`/`twoMove`/etc `3.5s infinite` — морфинг через clip-path
- **Задержки**: 0s, 0.15s, 0.3s, 0.575s, 0.725s, 0.875s — волновой эффект

---

## 🔍 Техническая проверка

### CSS подключён:
```bash
$ grep -c "page-loader-overlay" .next/static/css/*.css
f9097c1630393c6a.css:1  ✅
```

### Компонент корректный:
```tsx
// components/PageLoader.tsx
export default function PageLoader() {
  return (
    <div className="page-loader-overlay">
      <div className="page-loader-wrapper">
        <div className="page-loader-box-wrap">
          <div className="page-loader-box one"></div>
          <div className="page-loader-box two"></div>
          <div className="page-loader-box three"></div>
          <div className="page-loader-box four"></div>
          <div className="page-loader-box five"></div>
          <div className="page-loader-box six"></div>
        </div>
      </div>
    </div>
  )
}
```

### Время показа:
```tsx
// components/ResultsClient.tsx, строка 65
useEffect(() => {
  setTimeout(() => setIsLoading(false), 3000)
}, [])
```

---

## ⚠️ Возможная проблема на стороне клиента

**Симптом**: Пользователь видит "чёрный процессор" вместо белого лоадера.

**Причина**: **Кеш браузера** сохранил старую версию CSS/JS с прежним лоадером (микрочип).

**Решение**:
1. **Hard Refresh**: Ctrl+Shift+R (Windows/Linux) или Cmd+Shift+R (Mac)
2. **DevTools**: F12 → Network → Disable cache (галочка) → обновить страницу
3. **Очистка кеша**: Ctrl+Shift+Delete → "Кешированные изображения и файлы" → Очистить

**Проверка**: После hard refresh лоадер должен быть **белым с градиентными квадратиками**, а не чёрным с процессором.

---

## ✅ Заключение

**Лоадер работает корректно** согласно автоматизированным тестам Playwright:
- ✅ Появляется сразу после submit формы
- ✅ Показывается 3+ секунды
- ✅ Имеет белый полупрозрачный фон
- ✅ Содержит 6 анимированных градиентных квадратиков
- ✅ Плавно исчезает после загрузки данных

**Рекомендация**: Пользователь должен очистить кеш браузера (hard refresh).

---

**Артефакты сохранены в**: `/opt/deep-agg/docs/_artifacts/`
